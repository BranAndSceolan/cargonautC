import {router as vehicleRouter} from "./vehicle.router"
import {router as userRouter} from "./user.router"
import {router as evalRouter} from "./evaluation.router"
import {router as requestRouter} from "./request.router"
import {router as rideRouter} from "./ride.router"

export {
    vehicleRouter,
    userRouter,
    evalRouter,
    requestRouter,
    rideRouter
}